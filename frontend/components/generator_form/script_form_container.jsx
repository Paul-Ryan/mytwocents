import React from 'react';
import { connect } from 'react-redux';
import ScriptForm from './script_form';

const mapStateToProps = (state, ownProps) => {

  return (
    {}
  );
};

export default connect(
  mapStateToProps,
  null
)(ScriptForm);
